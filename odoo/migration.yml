migration:
  versions:
    - version: 9.1.0
      operations:
        post:
          - oerpscenario -t setup
          - anthem songs.install.accounting::main
    - version: 9.3.4
      addons:
        upgrade:
          - sale_discount_program
