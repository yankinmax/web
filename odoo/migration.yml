migration:
  versions:
    - version: 9.1.0
      operations:
        post:
          - oerpscenario -t setup
          - anthem songs.install.accounting::main
          - anthem songs.install.data::main
